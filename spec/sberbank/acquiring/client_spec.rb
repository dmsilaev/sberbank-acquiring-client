RSpec.describe Sberbank::Acquiring::Client do
  it "has a version number" do
    expect(Sberbank::Acquiring::Client::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
